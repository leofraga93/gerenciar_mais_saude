/**
 * Base de dados mock de serviços da clínica cadastrada.
 * Inicia vazia ([]) para permitir que a clínica cadastre apenas os exames e consultas pertinentes.
 */

export const DEFAULT_CLINIC_SERVICES = []
